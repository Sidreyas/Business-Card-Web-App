# Business Card OCR Environment Variables

# Groq AI API (Required for AI parsing)
# Get your API key from: https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# AWS Textract (Required for primary OCR)
# Create AWS IAM user with TextractFullAccess policy
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=us-east-1

# Google Cloud Vision (Optional - fallback OCR)
# Base64 encoded service account JSON
# To encode: base64 -i your-service-account.json
GOOGLE_APPLICATION_CREDENTIALS=your_base64_encoded_service_account_json_here

# Instructions:
# 1. Copy this file to .env.local for local development
# 2. Set these as environment variables in Vercel dashboard for production
# 3. Never commit actual API keys to git
